apiVersion: apps/v1
kind: Deployment
metadata:
  name: loksai-eta-deploy
  labels:
    app: netflix
spec:
  replicas: 3
  selector:
    matchLabels:
      app: netflix
  template:
    metadata:
      labels:
        app: netflix
    spec:
      containers:
      - name: netflix-container
        image: gowrisiva/netflix
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: netflix-np-service
  labels:
    app: netflix
spec:
  selector:
    app: netflix

  type: NodePort
  ports:
  - nodePort: 31028
    port: 8080
    targetPort: 8080
